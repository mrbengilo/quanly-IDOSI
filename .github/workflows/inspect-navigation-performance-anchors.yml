name: Inspect navigation performance anchors

on:
  push:
    branches:
      - perf/eliminate-navigation-event-loop-blocking
    paths:
      - .github/workflows/inspect-navigation-performance-anchors.yml

permissions:
  contents: read

jobs:
  inspect:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
      - name: Print exact source anchors
        shell: bash
        run: |
          set -Eeuo pipefail
          python - <<'PY'
          from pathlib import Path

          def print_block(path, needle, before=40, after=180):
              lines = Path(path).read_text(encoding='utf-8').splitlines()
              indexes = [i for i, line in enumerate(lines) if needle in line]
              print(f'\n===== {path} :: {needle} :: matches={len(indexes)} =====')
              for index in indexes:
                  start = max(0, index - before)
                  end = min(len(lines), index + after + 1)
                  print(f'--- lines {start + 1}-{end} ---')
                  for line_no in range(start, end):
                      print(f'{line_no + 1:06d}: {lines[line_no]}')

          print_block('server/worker.js', 'const requireSession = async', 20, 180)
          print_block('server/worker.js', 'const loadStateCollections = async', 20, 80)
          print_block('server/worker.js', 'const getEmployeeAccountAvatar = async', 110, 160)
          print_block('server/worker.js', 'const getIdentityImage = async', 10, 110)
          print_block('server/vps/sqlite-d1.mjs', 'constructor({ databasePath', 20, 80)
          print_block('src/services/employeeAvatarCache.js', 'const employeeAvatarCache', 10, 120)
          print_block('src/components/UI.jsx', 'loadEmployeeAvatarUrl', 20, 100)
          print_block('server/worker.test.js', 'EMPLOYEE_AVATAR_NOT_FOUND', 30, 70)
          PY
